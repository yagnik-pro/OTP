{
  "name": "meesho-otp-app",
  "version": "2.0.0",
  "description": "Multi-account Meesho seller return-OTP dashboard: email+password login, auto session, bulk relogin, account-wise + courier-wise OTPs",
  "main": "server.js",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.47.0"
  }
}
